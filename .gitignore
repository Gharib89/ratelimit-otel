# Written by /plugin-types from the running Claude Code build; regenerate with
# `claude -p "/plugin-types"` rather than committing a copy that goes stale.
.claude/types/

node_modules/

# The cc-otel prod read connection, for verifying that a delivery reached
# `raw.metrics`. Never committed: this repo is public.
.env
